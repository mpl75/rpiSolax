{
  "logDir": "/var/www/html/rpiSolax/logs",
  "authSecret": "REPLACE_WITH_RANDOM_HEX  (php -r 'echo bin2hex(random_bytes(32));')",
  "users": [
    {
      "user": "michal",
      "password": "$2y$10$REPLACE_WITH_BCRYPT_HASH",
      "admin": true
    }
  ]
}
